#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Prints the alphabet in lowercase, except for q and e.
 *
 * Return: Always 0.
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		/**
		* innitializing variable
		*/
	{
		printf("%d", i);
	}
	printf("\n");
		/**
		* adding new line
		*/
	return (0);


}
